# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
DB_SSLMODE=disable

# Optional: specify a config file instead of env vars
# CONFIG_FILE=config.json

# Logging
LOG_LEVEL=info